## Simple Proxy Server

This is a simple proxy server for bypass request

### Quick Start

1. copy `.env` file.
    ```
    cp .env.example .env
    ```
2. open `.env` file and update `HOST` to target url



